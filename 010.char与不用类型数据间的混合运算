#include<stdio.h>
int main()
{	
	char c1,c2;
	c1='A';
	c2=c1+32;
	printf("%c\n",c2);
	printf("%d\n",c2);
	return 0;
}



//字符（char）型数据与整形数据进行运算，就是把字符的ASCII代码与整型数据进行运算。
//如果字符数据与实型数据进行运算，则将字符的ASCII代码转换为double型数据，然后进行运算。
//以上转换是编译系统自动完成的，用户不必过问。
